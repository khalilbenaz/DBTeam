<Window x:Class="DBTeam.Modules.Debugger.Views.BreakpointConditionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="{DynamicResource Debugger.ConditionTitle}"
        Height="260" Width="520" WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Top">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <md:PackIcon Kind="CodeBraces" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource Brand.Primary}"/>
                <TextBlock Text="{DynamicResource Debugger.ConditionHeader}" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Text="{DynamicResource Debugger.ConditionHint}" Opacity="0.7" TextWrapping="Wrap" Margin="0,0,0,8"/>
            <TextBlock x:Name="LineLabel" FontFamily="Consolas" Opacity="0.6" Margin="0,0,0,4"/>
        </StackPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="{DynamicResource Common.Cancel}" Width="90" Margin="0,0,8,0" IsCancel="True" Click="Cancel_Click"/>
            <Button x:Name="OkBtn" Content="{DynamicResource Common.OK}" Width="90" IsDefault="True" Click="Ok_Click" Style="{StaticResource PrimaryButton}"/>
        </StackPanel>
        <TextBox x:Name="ConditionBox" FontFamily="Consolas" FontSize="13" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"/>
    </DockPanel>
</Window>
