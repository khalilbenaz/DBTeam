<UserControl x:Class="DBTeam.Modules.TableDesigner.Views.TableDesignerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">
    <DockPanel>
        <Border DockPanel.Dock="Top" Style="{StaticResource SectionHeader}">
            <StackPanel Orientation="Horizontal">
                <md:PackIcon Kind="TablePlus" Width="28" Height="28" VerticalAlignment="Center" Foreground="{StaticResource Brand.Primary}" Margin="0,0,12,0"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{DynamicResource Designer.Title}" Style="{StaticResource H1}"/>
                    <TextBlock Text="{DynamicResource Designer.Subtitle}" Style="{StaticResource Caption}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border DockPanel.Dock="Top" Padding="16,12" Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,8,0">
                    <TextBlock Text="{DynamicResource Designer.Connection}" Style="{StaticResource FieldLabel}"/>
                    <ComboBox ItemsSource="{Binding Connections}" DisplayMemberPath="Name" SelectedItem="{Binding Connection}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="0,0,8,0">
                    <TextBlock Text="{DynamicResource Designer.Database}" Style="{StaticResource FieldLabel}"/>
                    <ComboBox ItemsSource="{Binding Databases}" SelectedItem="{Binding Database}" IsEditable="True"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="0,0,8,0">
                    <TextBlock Text="{DynamicResource Designer.Schema}" Style="{StaticResource FieldLabel}"/>
                    <TextBox Text="{Binding Schema, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Grid.Column="3" Margin="0,0,8,0">
                    <TextBlock Text="{DynamicResource Designer.TableName}" Style="{StaticResource FieldLabel}"/>
                    <TextBox Text="{Binding TableName, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="8,0,0,0">
                    <Button Command="{Binding LoadExistingCommand}" Style="{StaticResource IconButton}" ToolTip="Load existing table" Margin="0,0,6,0">
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon Kind="Download" Margin="0,0,6,0"/>
                            <TextBlock Text="Load"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding GenerateDdlCommand}" Style="{StaticResource PrimaryButton}">
                        <StackPanel Orientation="Horizontal">
                            <md:PackIcon Kind="ScriptTextPlay" Margin="0,0,6,0"/>
                            <TextBlock Text="{DynamicResource Designer.GenerateDdl}"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <StatusBar DockPanel.Dock="Bottom">
            <md:PackIcon Kind="InformationOutline" VerticalAlignment="Center" Margin="0,0,6,0" Opacity="0.6"/>
            <StatusBarItem><TextBlock Text="{Binding Status}"/></StatusBarItem>
        </StatusBar>

        <Border Margin="8" Style="{StaticResource Card}" Padding="0">
            <DockPanel>
                <Border DockPanel.Dock="Top" Padding="8" BorderThickness="0,0,0,1" BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}">
                    <StackPanel Orientation="Horizontal">
                        <Button Command="{Binding AddColumnCommand}" Style="{StaticResource IconButton}" ToolTip="{DynamicResource Designer.AddColumn}">
                            <md:PackIcon Kind="Plus" Foreground="{StaticResource Brand.Success}"/>
                        </Button>
                        <Button Command="{Binding RemoveColumnCommand}" Style="{StaticResource IconButton}" ToolTip="{DynamicResource Designer.RemoveColumn}">
                            <md:PackIcon Kind="Minus" Foreground="{StaticResource Brand.Danger}"/>
                        </Button>
                        <Button Command="{Binding MoveUpCommand}" Style="{StaticResource IconButton}" ToolTip="{DynamicResource Designer.MoveUp}">
                            <md:PackIcon Kind="ArrowUp"/>
                        </Button>
                        <Button Command="{Binding MoveDownCommand}" Style="{StaticResource IconButton}" ToolTip="{DynamicResource Designer.MoveDown}">
                            <md:PackIcon Kind="ArrowDown"/>
                        </Button>
                    </StackPanel>
                </Border>
                <DataGrid ItemsSource="{Binding Columns}" SelectedItem="{Binding Selected}"
                          AutoGenerateColumns="False" CanUserAddRows="False"
                          HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource Designer.ColName}" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                        <DataGridTemplateColumn Header="{DynamicResource Designer.ColType}" Width="160">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox IsEditable="True"
                                              ItemsSource="{Binding DataContext.DataTypes, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              Text="{Binding DataType, UpdateSourceTrigger=LostFocus}" BorderThickness="0"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="{DynamicResource Designer.ColLength}" Binding="{Binding Length, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                        <DataGridCheckBoxColumn Header="{DynamicResource Designer.ColNullable}" Binding="{Binding IsNullable, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                        <DataGridCheckBoxColumn Header="{DynamicResource Designer.ColIdentity}" Binding="{Binding IsIdentity, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
                        <DataGridCheckBoxColumn Header="{DynamicResource Designer.ColPK}" Binding="{Binding IsPrimaryKey, UpdateSourceTrigger=PropertyChanged}" Width="50"/>
                        <DataGridTextColumn Header="{DynamicResource Designer.ColDefault}" Binding="{Binding DefaultExpression, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </DockPanel>
</UserControl>
